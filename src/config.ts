export const mysql = {
  "host": process.env.mysql_host,
  "user": process.env.mysql_user,
  "password": process.env.mysql_password,
  "database": process.env.mysql_database,
  "table": "sch_chenyunda218",
}

export const config = {
  "retry": 30
}